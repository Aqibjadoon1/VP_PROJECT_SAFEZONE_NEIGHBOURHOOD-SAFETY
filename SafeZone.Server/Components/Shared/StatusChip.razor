@using SafeZone.Server.Services

<span class="status-chip @Presentation.CssClass" title="@Presentation.Label">
    <span class="dot"></span>
    @Presentation.Label
</span>

@code {
    [Parameter] public object? Status { get; set; }

    private StatusPresentation Presentation => StatusPresenter.From(Status);
}
